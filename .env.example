# Local development only. Never commit real credentials.
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
API_PORT=8080
AIRFLOW_PORT=8081

# Required only when running the S3 raw-storage task.
RAW_S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
